//
//  SupplementaryTableViewController.swift
//  CatalogoSnippets
//
//  Created by Luciano Ferreira on 12/04/21.
//

import UIKit

class SupplementaryTableViewController: UITableViewController {
/*
        weak var delegate: SnippetSelectionDelegate?

        var snippets: [Snippet] = [
            Snippet(name: "Snippet 1", content: "let x = 10", tag: "Networking, Persistência"),
            Snippet(name: "Snippet 2", content: "let y = true", tag: "Networking, Persistência"),
            Snippet(name: "Snippet 3", content: "let z = \"abc\"", tag: "Networking, Persistência")
        ]
        

        override func viewDidLoad() {
            super.viewDidLoad()
        }

        override func tableView(
            _ tableView: UITableView,
            numberOfRowsInSection section: Int)
        -> Int {
            snippets.count
        }

        override func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
            let cell = tableView.dequeueReusableCell(withIdentifier: "Cell", for: indexPath)

            cell.textLabel?.text = snippets[indexPath.row].tag

            return cell
            
        }

        override func tableView(
            _ tableView: UITableView,
            didSelectRowAt indexPath: IndexPath) {
            let selectedSnippet = snippets[indexPath.row]
            delegate?.snippetSelected(selectedSnippet)

            if let supplementaryTableViewController = delegate as? SupplementaryTableViewController {
                splitViewController?.show(supplementaryTableViewController, sender: nil)
            }
        }
    */
    }

